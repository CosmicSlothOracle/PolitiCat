# Environment variables for Vite (client-side). Copy to ".env" and adjust for your environment.

# Signaling server WebSocket URL
# Local dev example:
VITE_SIGNALING_URL=ws://localhost:3001

# Optional TURN server for NAT traversal (recommended for production)
# Use secure 'turns' on 5349 if available, otherwise 'turn' on 3478
VITE_TURN_URL=turns:your-turn-server.example.com:5349
VITE_TURN_USERNAME=your_turn_username
VITE_TURN_CREDENTIAL=your_turn_password_or_token

# Notes:
# - VITE_* variables are embedded into the client bundle. Prefer time-limited credentials.
# - Netlify: set these under Site settings → Environment → Variables, then rebuild.

